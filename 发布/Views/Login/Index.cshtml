<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@(ViewBag.SoftName+ViewBag.SoftPubName)</title>
    <link href="~/Content/css/framework-font.css" rel="stylesheet" />
    <link href="~/Content/css/framework-login.css" rel="stylesheet" />
    <script src="~/Content/js/jquery/jquery-2.1.1.min.js"></script>
    <script src="~/Content/js/cookie/jquery.cookie.js"></script>
    <script src="~/Content/js/md5/jquery.md5.js"></script>
    <script src="~/Content/js/app.js"></script>
    <!--[if lte IE 8]>
        <div id="errorie"><div>您的IE已过时，正常使用系统前请升级您的浏览器到 IE8以上版本 <a target="_blank" href="http://windows.microsoft.com/zh-cn/internet-explorer/ie-8-worldwide-languages">点击升级</a>&nbsp;&nbsp;强烈建议您更改换浏览器：<a href="http://down.tech.sina.com.cn/content/40975.html" target="_blank">谷歌 Chrome</a></div></div>
    <![endif]-->
</head>
<body>
    <div class="wrapper" style="background-image:url('../../Content/img/background2.jpg')">
     
        <div class="containerbg" style="border-bottom: #666666 solid 1px; border-top: #666666 solid 1px; background-color: #00000096; height: 400px; position: relative; ">
            <div class="container">
                <div class="logo" style="display:flex;justify-content:center;align-items:center;padding:0px;margin: 0px 0px 10px 0px;height:60px;">
                    <img src="@ViewBag.LogoPath" style="flex-grow:1;max-width: @(ViewBag.LogoScale); max-height: @(ViewBag.LogoScale); object-fit: contain;margin-left:90px; " />
                    <h1 style="flex-grow: 6;text-align:left;padding-left:10px; line-height: 60px; font-size: 20px; text-shadow: none; ">@(ViewBag.SoftName+ViewBag.SoftPubName)</h1>
                </div>
                <form class="form" style="background-color: #000000b3; border-radius:20px;border:1px solid #d4d4d4;">
                    <div class="row">
                        <input id="txt_account" type="text" placeholder="输入用户名" value="">
                        <i class="fa fa-user"></i>
                    </div>
                    <div class="row">
                        <input id="txt_password" type="password" placeholder="输入密码" value="">
                        <i class="fa fa-key"></i>
                    </div>
                    <div class="row" style="display:none;">
                        <input id="txt_code" maxlength="4" type="text" placeholder="验证码" style="width: 190px; float: left;" value="-0001">
                        <div style=" float: right; padding-top: 14px; padding-left: 14px;">
                            <a id="switchCode" href="javascript:void();" style="text-decoration: none;">换一张</a>
                            <img id="imgcode" class="authcode" src="~/Login/GetAuthCode" width="80" height="25" />
                        </div>
                    </div>
                    <div class="row">
                        <button id="login_button" type="button"><span>登录</span></button>
                    </div>
                    <div class="row">
                    </div>
                </form>
                <div class="login_tips"></div>
            </div>
        </div>
        <ul class="bg-bubbles">
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
        </ul>
    </div>
    <div class="copyright" style="width:100%">
        <a href="@ViewBag.WebSite" target="_blank" style="text-decoration:none;color:#fff;">@ViewBag.CompanyName</a>
        <br>
        <a href="@ViewBag.GithubUrl" target="_blank" style="text-decoration:none;color:#fff; font-size:0.8rem">@ViewBag.Version</a>
    </div>
    <script type="text/javascript">

        function IsPC() {
            var userAgentInfo = navigator.userAgent;
            var Agents = new Array("Android", "iPhone", "SymbianOS", "Windows Phone", "iPad", "iPod");
            var flag = true;
            for (var v = 0; v < Agents.length; v++) {
                if (userAgentInfo.indexOf(Agents[v]) > 0) {
                    flag = false;
                    break;
                }
            }
            return flag;
        }

        (function ($) {
            $.login = {
                formMessage: function (msg) {
                    $('.login_tips').find('.tips_msg').remove();
                    $('.login_tips').append('<div class="tips_msg"><i class="fa fa-question-circle"></i>' + msg + '</div>');
                },
                loginClick: function () {
                    var $username = $("#txt_account");
                    var $password = $("#txt_password");
                    var $code = $("#txt_code");
                    if ($username.val() == "") {
                        $username.focus();
                        $.login.formMessage('请输入用户名/手机号/邮箱。');
                        return false;
                    } else if ($password.val() == "") {
                        $password.focus();
                        $.login.formMessage('请输入登录密码。');
                        return false;
                    }
                    else if ($code.val() == "") {
                        $code.focus();
                        $.login.formMessage('请输入验证码。');
                        return false;
                    }
                    else {
                        $("#login_button").attr('disabled', 'disabled').find('span').html("loading...");
                        $.ajax({
                            url: "/Login/CheckLogin",
                            data: { username: $.trim($username.val()), password: $.md5($.trim($password.val())), code: $.trim($code.val()), isNeedCode: true },
                            type: "post",
                            dataType: "json",
                            success: function (data) {
                                if (data.state == "success") {
                                    $("#login_button").find('span').html("登录成功，正在跳转...");
                                    window.setTimeout(function () {
                                        var isPc = IsPC();

                                        if (isPc) {
                                            window.location.href = "/Home/Index";
                                        }
                                        else {
                                            window.location.href = "/RFHome/Index";
                                        }
                                    }, 500);
                                } else {
                                    $("#login_button").removeAttr('disabled').find('span').html("登录");
                                    $("#switchCode").trigger("click");
                                    if ($code.val() != "-0001") {
                                        $code.val('');
                                    }
                                    $.login.formMessage(data.message);
                                }
                            }
                        });
                    }
                },
                init: function () {
                    $('.wrapper').height($(window).height());
                    $(".containerbg").css("margin-top", ($(window).height() - $(".container").height()) / 2 - 50);
                    $(window).resize(function (e) {
                        $('.wrapper').height($(window).height());
                        $(".containerbg").css("margin-top", ($(window).height() - $(".container").height()) / 2 - 50);
                    });
                    $("#switchCode").click(function () {
                        $("#imgcode").attr("src", "/Login/GetAuthCode?time=" + Math.random());
                    });
                    var login_error = top.$.cookie('MST_login_error');
                    if (login_error != null) {
                        switch (login_error) {
                            case "overdue":
                                $.login.formMessage("系统登录已超时,请重新登录");
                                break;
                            case "OnLine":
                                $.login.formMessage("您的帐号已在其它地方登录,请重新登录");
                                break;
                            case "-1":
                                $.login.formMessage("系统未知错误,请重新登录");
                                break;
                        }
                        top.$.cookie('MST_login_error', '', { path: "/", expires: -1 });
                    }
                    $("#login_button").click(function () {
                        $.login.loginClick();
                    });
                    document.onkeydown = function (e) {
                        if (!e) e = window.event;
                        if ((e.keyCode || e.which) == 13) {
                            document.getElementById("login_button").focus();
                            document.getElementById("login_button").click();
                        }
                    }
                }
            };
            $(function () {

                if (!@(ViewBag.IsForever.ToString().ToLower())== true) {
                    var dayNum = @ViewBag.DayNum;
                    if (dayNum<= 7) {
                        $.login.formMessage("许可证剩余" + dayNum+"天");
                    }
                }

                var islogin = top.$.cookie('MST_loginuserkey_2021');
                if (islogin != undefined && islogin != "") {
                    var isPc = IsPC();
                    if (isPc) {
                        window.location.href = "/Home/Index";
                    }
                    else {
                        window.location.href = "/RFHome/Index";
                    }
                }
                $.login.init();
            });

        })(jQuery);
    </script>
</body>
</html>
