@{
    ViewBag.Title = "Form";
    Layout = "~/Views/Shared/_Form.cshtml";
}
<script>
    var keyValue = $.request("keyValue");
    var areaId = $.request("areaId");
    var areaName = unescape($.request("areaName"));

    $(function () {
        initControl();

        $("#AreaName").val(areaName);
        if (!!keyValue) {
            $.ajax({
                url: "/PC_BaseDataManage/Item/GetFormJson",
                data: { keyValue: keyValue },
                dataType: "json",
                async: false,
                success: function (data) {
                    $.checkLogin(data);
                    $("#form1").formSerialize(data);
                }
            });
        }
    });

    function initControl() {
        $("#State").bindSelect({
            url: "/SystemManage/ItemsData/GetDicList?entityName=T_Location&fieldName=State",
            id: "F_ItemCode",
            text:"F_ItemName"
        });

        $("#ForbiddenState").bindSelect({
            url: "/SystemManage/ItemsData/GetDicList?entityName=T_Location&fieldName=ForbiddenState",
            id: "F_ItemCode",
            text: "F_ItemName"
        });
    }

    //单个新增
    function submitForm() {
        if (!$('#form1').formValid()) {
            return false;
        }
        var postData = $("#form1").formSerialize();
        postData["AreaID"] = areaId;
        postData["AreaName"] = areaName;
        postData["LocationType"] = "Flat";
        $.submitForm({
            url: "/PC_BaseDataManage/Location/GenFlatLocation",
            param: postData,
            success: function () {
                $.currentWindow().$("#gridList").trigger("reloadGrid");
            },
            error: function (data) {
                $.modalMsg(data.message, data.state);
            }
        })
    }
</script>

<form id="form1">
    <div style="margin-top: 10px; margin-left: 10px; margin-right: 10px;">
        <div style="padding-top: 20px; margin-right: 30px;">
            <table class="form">
                <tr>
                    <th class="formTitle">货位编码</th>
                    <td class="formValue">
                        <input id="LocationCode" name="LocationCode" type="text" class="form-control required" placeholder="请输入货位编码" />
                    </td>
                    <th class="formTitle">载重</th>
                    <td class="formValue">
                        <input id="Weight" name="Weight" type="number" min="0" class="form-control" placeholder="请输入载重" />
                    </td>
                </tr>
                <tr>
                    <th class="formTitle">货位高度</th>
                    <td class="formValue">
                        <input id="High" name="High" type="number" min="0" class="form-control" placeholder="请输入货位高度" />
                    </td>
                    <th class="formTitle">货位宽度</th>
                    <td class="formValue">
                        <input id="Width" name="Width" type="number" min="0" class="form-control" placeholder="请输入货位宽度" />
                    </td>
                </tr>
                <tr>
                    <th class="formTitle">货位状态</th>
                    <td class="formValue">
                        <select id="State" name="State" class="form-control required">
                            <option value="">==请选择==</option>
                        </select>
                    </td>
                    <th class="formTitle">禁用状态</th>
                    <td class="formValue">
                        <select id="ForbiddenState" name="ForbiddenState" class="form-control required">
                            <option value="">==请选择==</option>
                        </select>
                    </td>
                </tr>
                <tr>
                    <th class="formTitle">选项</th>
                    <td class="formValue">
                        <div class="ckbox">
                            <input id="IsBase" name="IsBase" type="checkbox" checked="checked"><label for="IsBase">系统数据</label>
                        </div>
                    </td>
                </tr>
            </table>
        </div>
    </div>
</form>

